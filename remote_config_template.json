{
  "parameters": {
    "enable_audio_notes": {
      "defaultValue": {
        "value": "true"
      },
      "valueType": "BOOLEAN",
      "description": "Enables or disables speech-to-text note creation."
    },
    "enable_ar_view": {
      "defaultValue": {
        "value": "true"
      },
      "valueType": "BOOLEAN",
      "description": "Toggles the availability of the Augmented Reality view."
    },
    "enable_qr_scanner": {
      "defaultValue": {
        "value": "true"
      },
      "valueType": "BOOLEAN",
      "description": "Toggles the availability of the QR code scanning mode."
    },
    "enable_emoji_pins": {
      "defaultValue": {
        "value": "true"
      },
      "valueType": "BOOLEAN",
      "description": "Allows users to select custom emojis for their note pins."
    },
    "max_note_length": {
      "defaultValue": {
        "value": "500"
      },
      "valueType": "NUMBER",
      "description": "The maximum character limit for notes."
    },
    "highlight_note_color": {
      "defaultValue": {
        "value": "#10b981"
      },
      "valueType": "STRING",
      "description": "The color used for highlighting markers and active UI elements."
    },
    "enable_moderation": {
      "defaultValue": {
        "value": "true"
      },
      "valueType": "BOOLEAN",
      "description": "Enables the user-facing report buttons and moderation system."
    },
    "enable_admin_hub": {
      "defaultValue": {
        "value": "true"
      },
      "valueType": "BOOLEAN",
      "description": "Toggles the visibility of the Moderation/Admin Hub for authorized users."
    },
    "risk_score_threshold": {
      "defaultValue": {
        "value": "40"
      },
      "valueType": "NUMBER",
      "description": "The risk score threshold at which content is automatically flagged for review."
    }
  }
}
